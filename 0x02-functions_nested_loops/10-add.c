#include "main.h"
/**
 * add - adds two integers and returns the result
 * @i: int to be added to i
 * @j: int to be added to j
 * Return: sum of i and j
 */

int add(int i, int j)
{
	int k;

	k = i + j;

	return (k);
}
